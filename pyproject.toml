[project]
name = "empo-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pettingzoo>=1.25.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
    "pykernel>=0.1.6",
]
